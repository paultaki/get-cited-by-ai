# Registered: paultakisaki.com GEO-hub vocabulary-floor test

- **Registered:** 2026-07-16 (this file's first commit date is the receipt; committed
  BEFORE the intervention it describes — the intervention ships in the commit
  immediately after this one, so the bet precedes the change).
- **Context:** The 2026-07-16 self-demand-probe (DataForSEO, single run) found the
  "getting cited by AI / GEO" topic PASSES the demand gate — ChatGPT grounded and
  cited external sources on all 3 target queries, not memory-answered — but
  paultakisaki.com was cited in **0 of 3**. Google Ads volume showed the topic's
  query demand sits under the aliases "AI SEO" (8,100/mo) and "answer engine
  optimization / AEO" (2,400/mo), while both "generative engine optimization" and
  "get cited by AI" returned no reportable volume. The hub page led with the
  low-volume phrasing and named none of the higher-demand aliases.
- **Intervention (the single discrete change under test):** On
  `paultakisaki.com/learn/how-to-get-cited-by-ai/`, add the aliases *answer engine
  optimization (AEO)* and *AI SEO* to the meta-description trio (meta / og /
  twitter) and to one visible opening-body sentence. The branded `<title>` and H1
  are left unchanged. NO change to demand, page depth, data, schema, or any other
  page — vocabulary floor ONLY, on one page, so any citation move is attributable to
  the floor plus time/recrawl and nothing else on-page. This is a deliberately
  *reasonable* floor (meta + visible body), not a maximal title-stuffed one.
- **Hypothesis (what the skill's own thesis predicts):** on-page vocabulary is a
  FLOOR, not a lever (A1 demand-selects; C1 schema-not-a-lever; B3 uncited pages
  carry more craft). The floor alone should NOT get the page cited. This registers
  the null on purpose: a confirmed 0 is public, timestamped evidence for the
  floor-not-lever claim, on the author's own property.
- **Instrument:** DataForSEO ChatGPT scraper — same 3 target queries and method as
  the 2026-07-16 baseline (baseline: paultakisaki.com cited in 0/3). Re-run on ≥3
  separate days in the scoring week (G6 — single runs are weather). Secondary, if
  paultakisaki.com is enrolled in Bing AI Performance by then: the grounding-queries
  export.
- **Scoreability floor:** requires (a) the ChatGPT-scraper instrument available, and
  (b) the edited page live AND recrawled by Bing (verify indexation of the updated
  copy before scoring). If either fails at the score date → VOID and re-window, not a
  miss.

| # | Prediction (band) | Refutation | Score-by | Outcome | Rules tested |
|---|---|---|---|---|---|
| 1 | Across the 3 baseline queries × ≥3 runs (≥9 cells), ChatGPT's grounded answers cite **paultakisaki.com in 0 cells** at the score-by date (unchanged from the 0/9 baseline) | **≥1 cell** cites paultakisaki.com. Per G8/G5 a refutation does NOT auto-credit the vocab floor — exogenous demand, time, and recrawl must be ruled out first — but ≥1 falsifies the strict "floor does nothing here" claim | 2026-08-31 | UNSCORED | A1, C1, B3 — is on-page vocabulary a floor or a lever, tested on the author's own hub |

- **Supplementary (recorded, NOT scored — no rigorous pre-baseline):** run the same
  battery for "answer engine optimization" and "AI SEO" queries (the newly added
  terms; assumed baseline 0 since the page contained neither string before
  2026-07-16). Context on whether the added vocabulary alone surfaces the page for
  the new-vocabulary queries.
- **Follow-up already scoped:** the LEVER test comes next and separately. If a later
  intervention adds genuine demand or original source-depth (not vocabulary), a new
  prediction will test whether THAT moves citations. Isolating the floor first is the
  whole point — one discrete change, one scoreable bet (G8).
