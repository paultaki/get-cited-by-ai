# Edit log — v1.0.1 (2026-07-15)

Discipline: no skill edit without a documented failing check (writing-skills Iron
Law, adapted: for wording/citation fixes the failing check is a verified mismatch
between the text and its own primary source; for behavior rules it is observed
failure in production runs). Every v1.0.1 change is listed with its check.

Two decorrelated post-release reviews produced the findings: an external model
review of the public repo, and a fresh-context adversarial pass over the skill's
first two production audits (credibilityos.ai, thefinalsloadout.com). All five
precision claims from the external review were verified against primary sources
before any edit; one of its recommendations was REJECTED (see bottom).

| # | File | Failing check (before) | Fix (after) |
|---|---|---|---|
| 1 | `modules/measurement-harness.md` | Cited G2 as the rule behind "primary free citation counter"; G2 is citations≠clicks, export mechanics live in G3 | Cites G3's mechanics for the reads, G2 for why referrals don't substitute |
| 2 | `README.md` | "one procedure per workflow step" — six steps, five modules (engine split lives in SKILL.md) | Reworded to match reality; engine-split location stated |
| 3 | `SKILL.md`, `README.md`, `RULEBOOK.md` header | "751-page controlled (template) experiment" — A1's own tag is OBSERVED-CORRELATIONAL; the design is a same-template observational regression, not a randomized experiment | "751-page same-template controlled regression (observational)" |
| 4 | `SKILL.md` | "replicated across two independent studies" — A1's receipt says "two independently-run studies converged": independently-run analyses of the same portfolio, not external replication | "converged on by two independently-run analyses (including an adversarial verification pass) of the same portfolio" |
| 5 | `modules/site-audit.md` (E1 item) | "a www/apex split cost one site 31.8% of its ChatGPT mentions" — E1's receipt: the mentions SAT on the www host and apex-only measurement undercounted them; nothing was lost | "left 31.8% … sitting on the alternate host — fragmenting entity identity and undercounting apex-only measurement" |
| 6 | `modules/site-audit.md` (D4 item) | "onset can run days instead of months (64 without IndexNow)" — dropped D4's INFERENTIAL qualifier; reads causal | Restored: observed spread + "causal attribution is INFERENTIAL, confounded by site and topic" |
| 7 | `templates/prediction-ledger.md` (new rule 5) | **Observed production failure, twice:** the skill's first two audits (different operators, same day) both registered predictions whose refutation condition was not the complement of the band — outcomes existed that fired neither. One gap was the rulebook's own documented modal outcome (A5) | Rule 5: band + refutation must partition the outcome space; name the statistic; set a scoreability floor |

New content (not edits): `predictions/` (public git-timestamped preregistration — 3
files), `examples/credibilityos-audit.md` (worked example incl. adversarial-pass log).

**Rejected recommendation:** adding a `modules/engine-split.md`. The Iron Law needs a
failing test; the evidence runs the other way — both production audits handled
step-2 camp-targeting correctly from SKILL.md alone (GREEN, twice). Revisit only if
a run fumbles the engine split.

**Queued (not done):** publishing raw baseline transcripts for
`tests/BASELINE-2026-07-15.md` (verbatim outputs exist; need a sanitization pass);
demand-probe module naming the "enrolled in GSC/GA4 but not in an AI-performance
instrument" trap (real candidate — it bit the credibilityos run — but no failing
*skill* test yet: the skill caught it at run time).
